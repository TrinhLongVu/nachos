#include "../userprog/syscall.h"

int main() {
    // char** argv = getArgv();
    // int id = getArgc();
    
    // PrintString(argv[0]);
    // PrintString(id);


    Create("test5.txt");
    // OpenFileId id = Open("test.txt", 1);
    // Write("hello", 7, id);
    return 0;
}